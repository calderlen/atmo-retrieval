name: retrieval
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      # Core modeling
      - exojax
      - jaxoplanet
      - jaxopt
      - pyfastchem==3.1.3
      - pysme-astro==1.0.2
      # Scientific stack
      - numpy
      - scipy
      - pandas
      - matplotlib
      - astropy
      - astroquery
      - h5py
      - xarray
      - tqdm
      - lightkurve
      - wotan
      - chromatic-lightcurves
      # Retrieval + diagnostics
      - numpyro
      - arviz
      - corner
      - emcee
      - dynesty
      - uncertainties
      # NVIDIA CUDA 12.x (Linux)
      - "jax[cuda12]"
